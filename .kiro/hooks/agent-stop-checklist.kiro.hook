{
  "enabled": true,
  "name": "执行结束检查清单",
  "description": "agent 每次执行结束时，检查是否有未归档的 track issue、是否执行了 auto_save、是否有已完成但未同步的 task update",
  "version": "1",
  "when": {
    "type": "agentStop"
  },
  "then": {
    "type": "askAgent",
    "prompt": "## 执行结束检查清单（必须逐项确认）\n\n1. 是否有活跃 track issue 未归档？如有，确认是否需要继续处理\n2. 步骤 I 是否完整执行？特别是：auto_save 偏好提取是否已调用\n3. 是否有已完成的代码任务但未调用 task update 同步数据库\n4. status 的 recent_changes 是否已更新"
  }
}